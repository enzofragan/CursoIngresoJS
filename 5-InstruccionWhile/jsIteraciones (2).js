function Mostrar()
{
	var contador;
	contador=10;

	while(contador>0)
	{
		contador --;//es lo mismo que decir contador - 1 y lo ismo con el ++.
		document.write("<br> "+contador);
	}

}//FIN DE LA FUNCIÓN